#NEXT
NEXT_PUBLIC_SITE_URL=http://localhost:3000

#APPWRITE
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT=
APPWRITE_DATABASE_ID=
APPWRITE_USER_COLLECTION_ID=
APPWRITE_BANK_COLLECTION_ID=
APPWRITE_TRANSACTION_COLLECTION_ID=
APPWRITE_SECRET=

#PLAID
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_PRODUCTS=auth,transactions,identity
PLAID_COUNTRY_CODES=US,CA
# If you use OAuth banks (e.g., First Platypus Bank), set this to an exact HTTPS URL
# that is also added to your Plaid dashboard's OAuth Redirect URIs list.
# Example when using a tunnel: https://abcd1234.ngrok.io
# NEXT_PUBLIC_PLAID_REDIRECT_URI=

# Dev-only: if Dwolla funding source creation fails, still save the bank document
# Set to 'true' only in development
# ALLOW_BANK_SAVE_WITHOUT_DWOLLA=false

# If your Dwolla sandbox requires On-Demand Authorization when creating funding sources
# set this to 'true' so _links are included in the request body
# DWOLLA_REQUIRE_ON_DEMAND=false

#DWOLLA
DWOLLA_KEY=
DWOLLA_SECRET=
DWOLLA_BASE_URL=https://api-sandbox.dwolla.com
DWOLLA_ENV=sandbox

#SENTRY (Optional - for error tracking)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=